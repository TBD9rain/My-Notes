# Lattice

## Diamond EDA

If a register without reset signal,
it is better to assign a initial value at its definition,
such as self-generated reset counter.

Otherwise,
the initial value is uncertain after power-on download.
